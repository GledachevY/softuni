class Point{
    constructor(x,y){
        this.x = x;
        this.y = y;
    }

    distance (a,b){
        if(typeof(a)){

        }
    }
}

const point = new Point(1,2);
console.log(typeof(point));